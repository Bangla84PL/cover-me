# ==================================
# SUPABASE CONFIGURATION
# ==================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!

# Your Supabase instance URL (e.g., https://api.supabase.smartcamp.ai)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here

# Supabase Anonymous Key (Public - safe to expose in frontend)
# This key is used for client-side operations with Row Level Security enabled
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Supabase Service Role Key (Private - NEVER expose in frontend!)
# This key bypasses Row Level Security and is used only on the server
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ==================================
# OPTIONAL CONFIGURATION
# ==================================

# JWT Secret (only needed if you're manually verifying tokens)
# SUPABASE_JWT_SECRET=your_jwt_secret_here

# Database Password (only needed for direct database connections)
# SUPABASE_DB_PASSWORD=your_db_password_here
# POSTGRES_PASSWORD=your_postgres_password_here
